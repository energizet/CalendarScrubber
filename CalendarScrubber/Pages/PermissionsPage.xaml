<?xml version="1.0" encoding="utf-8"?>

<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="CalendarScrubber.Pages.PermissionsPage"
             Title="Доступы и Разрешения"
             BackgroundColor="{AppThemeBinding Light=#F5F5F5, Dark=#121212}">
    <ContentPage.Resources>
        <Style TargetType="Frame">
            <Setter Property="BackgroundColor" Value="{AppThemeBinding Light=White, Dark=#1E1E1E}" />
            <Setter Property="BorderColor" Value="Transparent" />
            <Setter Property="CornerRadius" Value="10" />
            <Setter Property="Padding" Value="15" />
        </Style>
        <Style TargetType="Label" x:Key="TitleLabel">
            <Setter Property="TextColor" Value="{AppThemeBinding Light=Black, Dark=White}" />
            <Setter Property="FontAttributes" Value="Bold" />
            <Setter Property="FontSize" Value="16" />
        </Style>
    </ContentPage.Resources>

    <ScrollView>
        <VerticalStackLayout Padding="20" Spacing="15">

            <Label Text="Для надежной работы будильника приложению требуются следующие разрешения:"
                   TextColor="Gray" FontSize="14" HorizontalTextAlignment="Center" Margin="0,0,0,10" />

            <Frame x:Name="FrameNotifications">
                <Grid RowDefinitions="Auto, Auto, Auto" ColumnDefinitions="*, Auto" RowSpacing="5">
                    <Label Grid.Row="0" Grid.Column="0" Text="🔔 Уведомления" Style="{StaticResource TitleLabel}" />
                    <Label Grid.Row="0" Grid.Column="1" x:Name="StatusNotifications" Text="Checking..." FontSize="12"
                           FontAttributes="Bold" TextColor="{AppThemeBinding Light=Black, Dark=#E0E0E0}" />

                    <Label Grid.Row="1" Grid.ColumnSpan="2"
                           Text="Чтобы показывать оповещения о предстоящих событиях в шторке." TextColor="Gray"
                           FontSize="12" />

                    <Button Grid.Row="2" Grid.ColumnSpan="2" Text="Разрешить" Clicked="OnRequestNotifications"
                            Margin="0,10,0,0" />
                </Grid>
            </Frame>

            <Frame x:Name="FrameOverlay">
                <Grid RowDefinitions="Auto, Auto, Auto" ColumnDefinitions="*, Auto" RowSpacing="5">
                    <Label Grid.Row="0" Grid.Column="0" Text="🛡️ Поверх приложений"
                           Style="{StaticResource TitleLabel}" />
                    <Label Grid.Row="0" Grid.Column="1" x:Name="StatusOverlay" Text="Checking..." FontSize="12"
                           FontAttributes="Bold" TextColor="{AppThemeBinding Light=Black, Dark=#E0E0E0}" />

                    <Label Grid.Row="1" Grid.ColumnSpan="2"
                           Text="Чтобы показывать экран будильника, даже если телефон заблокирован."
                           TextColor="Gray" FontSize="12" />

                    <Button Grid.Row="2" Grid.ColumnSpan="2" Text="Настроить" Clicked="OnRequestOverlay"
                            Margin="0,10,0,0" />
                </Grid>
            </Frame>

            <Frame x:Name="FrameAlarm">
                <Grid RowDefinitions="Auto, Auto, Auto" ColumnDefinitions="*, Auto" RowSpacing="5">
                    <Label Grid.Row="0" Grid.Column="0" Text="⏰ Точные будильники" Style="{StaticResource TitleLabel}" />
                    <Label Grid.Row="0" Grid.Column="1" x:Name="StatusAlarm" Text="Checking..." FontSize="12"
                           FontAttributes="Bold" TextColor="{AppThemeBinding Light=Black, Dark=#E0E0E0}" />

                    <Label Grid.Row="1" Grid.ColumnSpan="2"
                           Text="Чтобы сигнал срабатывал ровно в назначенную минуту, а не с задержкой."
                           TextColor="Gray" FontSize="12" />

                    <Button Grid.Row="2" Grid.ColumnSpan="2" Text="Разрешить" Clicked="OnRequestAlarm"
                            Margin="0,10,0,0" />
                </Grid>
            </Frame>

            <Frame x:Name="FrameBattery">
                <Grid RowDefinitions="Auto, Auto, Auto" ColumnDefinitions="*, Auto" RowSpacing="5">
                    <Label Grid.Row="0" Grid.Column="0" Text="🔋 Без ограничений" Style="{StaticResource TitleLabel}" />
                    <Label Grid.Row="0" Grid.Column="1" x:Name="StatusBattery" Text="Checking..." FontSize="12"
                           FontAttributes="Bold" TextColor="{AppThemeBinding Light=Black, Dark=#E0E0E0}" />

                    <Label Grid.Row="1" Grid.ColumnSpan="2"
                           Text="Разрешить работу в фоне без ограничений батареи, чтобы система не убивала сервис."
                           TextColor="Gray" FontSize="12" />

                    <Button Grid.Row="2" Grid.ColumnSpan="2" Text="Отключить экономию" Clicked="OnRequestBattery"
                            Margin="0,10,0,0" />
                </Grid>
            </Frame>

            <Frame x:Name="FrameFullScreen">
                <Grid RowDefinitions="Auto, Auto, Auto" ColumnDefinitions="*, Auto" RowSpacing="5">
                    <Label Grid.Row="0" Grid.Column="0" Text="📱 На весь экран" Style="{StaticResource TitleLabel}" />
                    <Label Grid.Row="0" Grid.Column="1" x:Name="StatusFullScreen" Text="Checking..." FontSize="12"
                           FontAttributes="Bold" TextColor="{AppThemeBinding Light=Black, Dark=#E0E0E0}" />

                    <Label Grid.Row="1" Grid.ColumnSpan="2"
                           Text="Требуется для Android 14+, чтобы разворачивать будильник из шторки." TextColor="Gray"
                           FontSize="12" />

                    <Button Grid.Row="2" Grid.ColumnSpan="2" Text="Разрешить" Clicked="OnRequestFullScreen"
                            Margin="0,10,0,0" />
                </Grid>
            </Frame>

            <Frame x:Name="FrameAutoStart" BorderColor="#512BD4">
                <Grid RowDefinitions="Auto, Auto, Auto" ColumnDefinitions="*, Auto" RowSpacing="5">
                    <Label Grid.Row="0" Grid.Column="0" Text="🚀 Автозапуск" Style="{StaticResource TitleLabel}" />
                    <Label Grid.Row="0" Grid.Column="1" Text="Проверьте вручную" FontSize="10" TextColor="Gray"
                           FontAttributes="Italic" />

                    <Label Grid.Row="1" Grid.ColumnSpan="2"
                           Text="Критично для Xiaomi/Huawei/Oppo! Без этого будильник не сработает после перезагрузки."
                           TextColor="{AppThemeBinding Light=Black, Dark=#E0E0E0}" FontSize="12" />

                    <Button Grid.Row="2" Grid.ColumnSpan="2" Text="Открыть настройки автозапуска"
                            Clicked="OnRequestAutoStart" Margin="0,10,0,0"
                            BackgroundColor="#512BD4" TextColor="White" />
                </Grid>
            </Frame>

            <Frame x:Name="FrameLockScreen" BorderColor="#512BD4">
                <Grid RowDefinitions="Auto, Auto, Auto" ColumnDefinitions="*, Auto" RowSpacing="5">
                    <Label Grid.Row="0" Grid.Column="0" Text="🔒 На экране блокировки"
                           Style="{StaticResource TitleLabel}" />
                    <Label Grid.Row="0" Grid.Column="1" Text="Проверьте вручную" FontSize="10" TextColor="Gray"
                           FontAttributes="Italic" />

                    <Label Grid.Row="1" Grid.ColumnSpan="2"
                           Text="Критично для Xiaomi (MIUI)! Разрешает показывать будильник, когда телефон заблокирован."
                           TextColor="{AppThemeBinding Light=Black, Dark=#E0E0E0}" FontSize="12" />

                    <Button Grid.Row="2" Grid.ColumnSpan="2" Text="Настроить (Другие разрешения)"
                            Clicked="OnRequestLockScreen" Margin="0,10,0,0"
                            BackgroundColor="#512BD4" TextColor="White" />
                </Grid>
            </Frame>

            <Button Text="⚙️ Открыть все настройки приложения"
                    Clicked="OnOpenSettingsClicked"
                    BackgroundColor="Transparent" TextColor="#512BD4"
                    Margin="0,20,0,0" />

            <Button x:Name="ContinueBtn"
                    Text="Продолжить"
                    Clicked="OnContinueClicked"
                    BackgroundColor="#512BD4"
                    TextColor="White"
                    FontAttributes="Bold"
                    HeightRequest="50"
                    CornerRadius="25"
                    Margin="0,20,0,30"
                    IsVisible="False" />
        </VerticalStackLayout>
    </ScrollView>
</ContentPage>