<?xml version="1.0" encoding="utf-8"?>

<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:toolkit="http://schemas.microsoft.com/dotnet/2022/maui/toolkit"
             x:Class="CalendarScrubber.Pages.MainPage"
             Title="Монитор Событий"
             BackgroundColor="{AppThemeBinding Light=#F5F5F5, Dark=#121212}">
    <ContentPage.Resources>
        <ResourceDictionary>
            <toolkit:IsStringNotNullOrEmptyConverter x:Key="IsNotNullOrEmptyConverter" />
        </ResourceDictionary>
    </ContentPage.Resources>

    <ContentPage.ToolbarItems>
        <ToolbarItem Text="Настройки" Clicked="OnSettingsClicked" Order="Primary" />
    </ContentPage.ToolbarItems>

    <Grid RowDefinitions="Auto, Auto, Auto, *, Auto"
          Padding="20"
          RowSpacing="15">

        <VerticalStackLayout Grid.Row="0" Spacing="5">
            <Label Text="📅 Календарь"
                   FontSize="28"
                   FontAttributes="Bold"
                   HorizontalOptions="Center"
                   TextColor="{AppThemeBinding Light=#1f1f1f, Dark=White}" />
            <Label Text="Автоматическая проверка событий"
                   FontSize="14"
                   TextColor="{AppThemeBinding Light=Gray, Dark=#A0A0A0}"
                   HorizontalOptions="Center" />
        </VerticalStackLayout>

        <Button x:Name="RunBtn"
                IsVisible="False"
                Grid.Row="0"
                Text="Run"
                Clicked="RunClicked"
                BackgroundColor="#512BD4"
                TextColor="White"
                HeightRequest="50"
                CornerRadius="12"
                FontAttributes="Bold"
                Shadow="{Shadow Brush=Black, Offset='2,2', Opacity=0.2}" />

        <Button x:Name="LoginBtn"
                Grid.Row="1"
                Text="Обновить"
                Clicked="OnLoginClicked"
                BackgroundColor="#512BD4"
                TextColor="White"
                HeightRequest="50"
                CornerRadius="12"
                FontAttributes="Bold"
                Shadow="{Shadow Brush=Black, Offset='2,2', Opacity=0.2}" />

        <Frame Grid.Row="2"
               CornerRadius="8"
               Padding="15"
               BackgroundColor="{AppThemeBinding Light=White, Dark=#1E1E1E}"
               BorderColor="{AppThemeBinding Light=#E0E0E0, Dark=#333333}">
            <HorizontalStackLayout HorizontalOptions="Center" Spacing="10">
                <ActivityIndicator x:Name="LoadingSpinner"
                                   IsRunning="False"
                                   Color="#512BD4"
                                   HeightRequest="20"
                                   WidthRequest="20" />
                <Label x:Name="StatusLabel"
                       Text="Ожидание авторизации..."
                       VerticalOptions="Center"
                       TextColor="{AppThemeBinding Light=#333, Dark=#E0E0E0}" />
            </HorizontalStackLayout>
        </Frame>

        <CollectionView x:Name="EventsCollection"
                        Grid.Row="3"
                        SelectionMode="Single">

            <CollectionView.EmptyView>
                <VerticalStackLayout VerticalOptions="Center" HorizontalOptions="Center" Spacing="10">
                    <Label Text="🔕" FontSize="40" HorizontalOptions="Center" />
                    <Label Text="Событий нет" TextColor="Gray" />
                </VerticalStackLayout>
            </CollectionView.EmptyView>

            <CollectionView.ItemTemplate>
                <DataTemplate>
                    <Frame Margin="0,5"
                           Padding="15"
                           CornerRadius="12"
                           BackgroundColor="{AppThemeBinding Light=White, Dark=#1E1E1E}"
                           BorderColor="{AppThemeBinding Light=#F0F0F0, Dark=#333333}"
                           HasShadow="True">

                        <Grid ColumnDefinitions="70, *" RowDefinitions="Auto, Auto, Auto">

                            <VerticalStackLayout Grid.Column="0" Grid.RowSpan="3" VerticalOptions="Start">
                                <Label Text="{Binding DisplayDate}"
                                       IsVisible="{Binding DisplayDate, Converter={StaticResource IsNotNullOrEmptyConverter}}"
                                       FontSize="11"
                                       FontAttributes="Bold"
                                       TextColor="{AppThemeBinding Light=#D32F2F, Dark=#FF5252}" 
                                       HorizontalOptions="Start"
                                       Margin="0,0,0,2"/>
                                <Label Text="{Binding LocalStart, StringFormat='{0:HH:mm}'}"
                                       TextColor="{Binding TimeColor}"
                                       FontAttributes="Bold"
                                       FontSize="18" />
                                <Label Text="{Binding LocalEnd, StringFormat='{0:HH:mm}'}"
                                       TextColor="Gray"
                                       FontSize="13" />
                            </VerticalStackLayout>

                            <Label Grid.Column="1" Grid.Row="0"
                                   Text="{Binding DisplaySubject}"
                                   TextColor="{AppThemeBinding Light={Binding UiColor}, Dark=White}"
                                   FontSize="16"
                                   FontAttributes="Bold"
                                   LineBreakMode="TailTruncation" />

                            <Label Grid.Column="1" Grid.Row="1"
                                   Text="{Binding Preview}"
                                   TextColor="{AppThemeBinding Light={Binding UiColor}, Dark=#D0D0D0}"
                                   FontSize="12"
                                   MaxLines="2"
                                   LineBreakMode="TailTruncation"
                                   IsVisible="{Binding Preview, Converter={StaticResource IsNotNullOrEmptyConverter}}"
                                   Margin="0,2,0,0" />

                            <HorizontalStackLayout Grid.Column="1" Grid.Row="2"
                                                   Spacing="5"
                                                   Margin="0,4,0,0"
                                                   IsVisible="{Binding Location, Converter={StaticResource IsNotNullOrEmptyConverter}}">
                                <Label Text="📍" FontSize="12" TextColor="{Binding UiColor}" />
                                <Label Text="{Binding Location}"
                                       FontSize="11"
                                       TextColor="{Binding UiColor}"
                                       LineBreakMode="TailTruncation" />
                            </HorizontalStackLayout>

                        </Grid>
                    </Frame>
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>

        <VerticalStackLayout Grid.Row="4" Spacing="5">
            <Button x:Name="LogToggleBtn"
                    Text="📜 Показать логи"
                    Clicked="OnToggleLogsClicked"
                    BackgroundColor="Transparent"
                    TextColor="Gray"
                    FontSize="12"
                    HorizontalOptions="Center"
                    HeightRequest="30"
                    Padding="0" />

            <Frame x:Name="LogsFrame"
                   IsVisible="False"
                   HeightRequest="200"
                   Padding="10"
                   BackgroundColor="{AppThemeBinding Light=#F0F0F0, Dark=#252525}"
                   CornerRadius="10"
                   HasShadow="False">

                <CollectionView x:Name="LogsCollection">
                    <CollectionView.ItemTemplate>
                        <DataTemplate>
                            <Grid ColumnDefinitions="65, *">
                                <Label Text="{Binding DisplayTime}" FontSize="10" TextColor="#666" />
                                <Label Grid.Column="1" Text="{Binding Message}" FontSize="10"
                                       TextColor="{AppThemeBinding Light=#333, Dark=#CCCCCC}"
                                       LineBreakMode="WordWrap" />
                            </Grid>
                        </DataTemplate>
                    </CollectionView.ItemTemplate>
                </CollectionView>
            </Frame>
        </VerticalStackLayout>
    </Grid>
</ContentPage>